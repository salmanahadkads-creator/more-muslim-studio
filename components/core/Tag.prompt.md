Small uppercase metadata chip with sharp corners (the brand is angular). Only approved colour pairings — no terracotta or coastal-blue tints.

```jsx
<Tag>S1 · E7</Tag>                     {/* outline — transparent, ivory border/text on night */}
<Tag variant="filled">New</Tag>         {/* filled — harvest bg + oak text (pairing 4) */}
<Tag variant="ivory">Documentary</Tag>  {/* ivory bg + oak text (pairing 3) */}
<Tag variant="mist">Profile</Tag>       {/* mist bg + night text (pairing 5) */}
```

Variants: `outline`, `filled` (harvest+oak), `ivory` (ivory+oak), `mist` (mist+night).
